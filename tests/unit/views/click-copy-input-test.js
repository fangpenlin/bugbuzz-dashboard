import { moduleFor, test } from 'ember-qunit';

moduleFor('view:click-copy-input', 'Unit | View | click copy input');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
